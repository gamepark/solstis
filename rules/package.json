{
  "name": "@gamepark/solstis",
  "version": "1.1.0",
  "description": "The rules of Solstis adapted for Game Park",
  "sideEffects": false,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "author": "Romain Fromi <romain@game-park.com> (https://game-park.com/)",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "vitest",
    "check:cycles": "madge --circular --extensions ts src",
    "build": "yarn check:cycles && tsc",
    "prepack": "yarn build",
    "publish": "yarn version ${0:-patch} && yarn npm publish && git commit -am v$(node -p \"require('./package.json').version\") && git tag v$(node -p \"require('./package.json').version\") && git push --follow-tags",
    "lint": "eslint --fix"
  },
  "peerDependencies": {
    "@gamepark/rules-api": ">=7",
    "es-toolkit": "^1.45.1",
    "lodash": ">=4.17"
  },
  "devDependencies": {
    "@gamepark/rules-api": "~7.6.3",
    "@types/lodash": "^4.17.7",
    "es-toolkit": "^1.47.0",
    "lodash": "^4.17.21",
    "madge": "^8.0.0",
    "vitest": "^4.1.8"
  }
}
